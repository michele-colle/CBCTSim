# Baseline twin of test_denoise.cfg — identical geometry, denoise OFF.
# Compare its .raw against the denoised one slice-by-slice.
# Run from the repo root:  build/dicom_to_mcgpu denoise_checks/test_baseline.cfg
# NOTE: output_prefix is resolved relative to the DICOM dir's parent unless
# absolute (see dicom_to_mcgpu.cpp), so keep it absolute.
dicom_dir      = "/mnt/f/Michele_diskF/Test Fantoccio Zurigo-CBCTvsCT/patient_ct_registered"
output_prefix  = "/home/colle/CBCTSim/ICRPPhantomToMCGPU/denoise_checks/output/baseline/MCGPU"

vol_vxy_mm    = 0.3
vol_vz_mm     = 0.3
vol_width_mm  = 640.0
vol_height_mm = 640.0
vol_length_mm = 30.0

thr_air_fat        = -500
thr_fat_soft       =  -50
thr_soft_spongiosa =  600
thr_spongiosa_cort =  601

denoise                = false
median_radius          = 0
despeckle_min_size     = 0

write_dicom = false
stretcher_enable = false
implant_count = 0
